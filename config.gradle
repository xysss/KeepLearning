ext {
    /*cfgs = [
            compileSdkVersion        : 30,
            buildToolsVersion        : "30.0.2",
            minSdkVersion            : 21,
            targetSdkVersion         : 30,
            versionCode              : 2,
            versionName              : "1.0.2",
            testInstrumentationRunner: "androidx.test.runner.AndroidJUnitRunner",
            consumerProguardFiles    : 'consumer-rules.pro',
    ]*/

    sign = [
            file         : rootProject.getRootDir().getAbsolutePath() + '/xys.jks',
            storePassword: 'xys123456',
            keyAlias     : 'xys',
            keyPassword  : 'xys123456'
    ]
    roomVersion = '2.2.5'
}